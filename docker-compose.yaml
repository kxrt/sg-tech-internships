services:
  # service to get intership data
  backend:
    build: 
      context: ./backend
    ports:
      - "8000:8080"

  # frontend service
  frontend:
    build: 
      context: ./frontend
    ports:
      - "8080:5173"
    depends_on:
      - backend